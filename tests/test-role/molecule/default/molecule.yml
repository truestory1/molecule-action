---
lint: |
  set -e
  yamllint .
  ansible-lint -f full
dependency:
  name: galaxy
  options:
    # ignore-certs: True
    # ignore-errors: True
    role-file: requirements.yml
    requirements-file: requirements.yml
    force: false

driver:
  name: containers
platforms:
  - name: testrole01
    pre_build_image: true
    # renovate: datasource=docker
    image: rockylinux/rockylinux:10-ubi-init@sha256:6c0ce6cdbe6fecb88598a0656e6ca60b93ba1a4216c166b42317e6477cdfdecb
    privileged: true
    command: "/usr/sbin/init"
    tmpfs:
      - /run
      - /run/lock
    volumes:
      - "/sys/fs/cgroup:/sys/fs/cgroup:rw"
provisioner:
  name: ansible
  config_options:
    defaults:
      roles_path: "$MOLECULE_PROJECT_DIRECTORY/.."
verifier:
  name: ansible
